console.log('iniciando os trabalhos')


// Dentro do javaescript existe uma diferença entre a execução de um script pelo node 
// ou pelo navegador. o objeto global tem esse mesmo nome no node, no entanto no 
// navegador, o mesmo objeto se chama window.


// O let cria a variável no escopo local enquando o var cria no objeto global. 

// O javascript é baseado em funcões, até mesmo as classes são funções por debaixo
// dos panos
